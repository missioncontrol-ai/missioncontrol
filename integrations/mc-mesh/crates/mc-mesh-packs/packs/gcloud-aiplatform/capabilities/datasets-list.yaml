name: gcloud.aiplatform.datasets.list
version: 1
description: "List Vertex AI datasets in a GCP project"
backend:
  type: subprocess
  command: gcloud
  args:
    - ai
    - datasets
    - list
    - "--project={{ input.project }}"
    - "--region={{ input.region }}"
    - "--format=json"
risk: low
sideEffectClass: readOnly
inputSchema:
  type: object
  required: [project, region]
  properties:
    project:
      type: string
      description: "GCP project ID"
    region:
      type: string
      description: "GCP region (e.g. us-central1)"
