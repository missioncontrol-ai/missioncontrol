services:
  mosquitto:
    image: eclipse-mosquitto:2
    container_name: missioncontrol-mosquitto
    restart: unless-stopped
    volumes:
      - ./docker/mosquitto/mosquitto.dev.conf:/mosquitto/config/mosquitto.conf:ro
    healthcheck:
      test: ["CMD-SHELL", "mosquitto_sub -C 1 -t '$SYS/broker/version' -i healthcheck >/dev/null 2>&1 || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 8
      start_period: 20s
    mem_limit: 256m
    cpus: 1.0

  rustfs:
    image: rustfs/rustfs:latest
    container_name: missioncontrol-rustfs
    restart: unless-stopped
    environment:
      RUSTFS_ACCESS_KEY: missioncontrol
      RUSTFS_ACCESS_SECRET: missioncontrol-secret
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - missioncontrol-rustfs-data:/data
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:9000/health/live || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 10s
    mem_limit: 512m
    cpus: 1.0

  rustfs-init:
    image: minio/mc:latest
    container_name: missioncontrol-rustfs-init
    depends_on:
      rustfs:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
        mc alias set local http://rustfs:9000 missioncontrol missioncontrol-secret &&
        mc mb --ignore-existing local/missioncontrol &&
        echo 'bucket ready'
      "
    restart: "no"

  api:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: missioncontrol-api
    depends_on:
      mosquitto:
        condition: service_healthy
      rustfs-init:
        condition: service_completed_successfully
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DATABASE_URL: sqlite:////data/taskman.db
      VECTOR_STORE_BACKEND: chroma
      MC_DB_RUNTIME_MIGRATIONS: "true"
      MQTT_HOST: mosquitto
      MQTT_PORT: "1883"
      MQTT_USERNAME: ""
      MQTT_PASSWORD: ""
      MC_OBJECT_STORAGE_ENDPOINT: http://rustfs:9000
      MC_OBJECT_STORAGE_REGION: us-east-1
      MC_OBJECT_STORAGE_BUCKET: missioncontrol
      MC_OBJECT_STORAGE_SECURE: "false"
      MC_OBJECT_STORAGE_ACCESS_KEY: missioncontrol
      MC_OBJECT_STORAGE_ACCESS_SECRET: missioncontrol-secret
    ports:
      - "8008:8008"
    volumes:
      - ./backend/chroma:/app/chroma
      - ./backend/dev-data:/data
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8008/readyz || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 8
      start_period: 25s
    mem_limit: 1g
    cpus: 1.5

  mcpd:
    build:
      context: .
      dockerfile: integrations/missioncontrol-mcp/Dockerfile
    container_name: missioncontrol-mcpd
    depends_on:
      api:
        condition: service_healthy
    restart: unless-stopped
    environment:
      MC_MCP_MODE: daemon
      MC_BASE_URL: http://api:8008
      MC_DAEMON_HOST: 0.0.0.0
      MC_DAEMON_PORT: "8765"
      MC_TOKEN: ${MC_TOKEN:-}
    ports:
      - "8765:8765"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8765/healthz || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 8
      start_period: 20s
    mem_limit: 256m
    cpus: 1.0

volumes:
  missioncontrol-rustfs-data:
